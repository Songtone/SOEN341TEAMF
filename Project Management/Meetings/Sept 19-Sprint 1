$ git clone (copy clone from github)
$ cd (into file)
$ git checkout -b (name) creates a branch named name
Modify files
$ git add .
$ git commit -am “message”
$ git push 

Got to github
Create pull request
When you push, go to pull request to add people to review it
If good you merge into master branch


Odd cases: 
$ git status
$ git add .
$git commit -m “message”
$git push
If upstream message, copy it and paste and run.

To update branch:
$ git checkout master
$ git pull
$ git checkout branch
$ git merge master
